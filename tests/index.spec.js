/* eslint-disable */
import chai, { expect } from 'chai'
import parser from '../lib/index'

chai.should()

describe('../lib/index', () => {
  describe('', () => {
    it('', (done) => {
      done()
    })
  })
})